<%= form_for(@accept) do |f| %>
  <% if @accept.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accept.errors.count, "error") %> prohibited this accept from being saved:</h2>

      <ul>
      <% @accept.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="pull-right">
    <%= f.hidden_field :terms_and_condition_id %>
    <%= link_to "Decline", notes_path, class: 'btn btn-danger' %>
    <%= f.submit "Accept", class: "btn btn-success" %>
  </div>
<% end %>
